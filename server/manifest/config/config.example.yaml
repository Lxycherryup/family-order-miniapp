server:
  address: ":8000"
  openapiPath: "/api.json"
  swaggerPath: "/swagger"

database:
  default:
    link: "pgsql:family_order:family_order@tcp(127.0.0.1:5432)/family_order?sslmode=disable"

jwt:
  secret: "please-change-me"
  miniappExpireHours: 168
  adminExpireHours: 24

wechat:
  appId: "your-miniapp-app-id"
  appSecret: "your-miniapp-app-secret"
